using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace recursive_robocopy
{
    class Program
    {
        
        static void Main(string[] args)
        {
            try
            {
                // simply pass everything in to the internal class 
                var recursiveRobocopy = new recursiveRobocopyProgram(args);
                recursiveRobocopy.startRoboCopy();
            }
            catch (Exception ex)
            {
                // do nothing and stop
                return;
            }
        }
    }
}
